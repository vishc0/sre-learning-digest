# Cross-Layer Dependency Map — Template

## Instructions
One map per service. Complete all five sections. Section 2 and Section 3 must be exhaustive — incomplete dependency maps are a governance gap. Section 4 is the SPOF risk assessment that the VP reads first in an incident or architecture review. Section 5 is the visual confirmation of Sections 2 and 3.

---

## Section 1: Service Record

| Field | Value |
|---|---|
| Service Name | [exact CMDB CI name] |
| Layer | [Layer Number] — [Layer Name] |
| Owning Team | [team name] |
| Criticality | [Tier 1 — SEV1 if down / Tier 2 — SEV2 if down / Tier 3 — degraded experience only] |
| SLO | [availability % / latency P99 ms / error rate %] |
| Map Owner | [name — who maintains this document] |
| Last Verified | [YYYY-MM-DD — date dependencies were last confirmed accurate] |

---

## Section 2: Upstream Dependencies (what this service calls)

List every external call this service makes — synchronous and asynchronous. No exceptions. A missing dependency discovered during an incident is a process failure.

| Dependency Name | Layer | Dependency Type | Protocol | SLO of Dependency | Circuit Breaker Configured? | Fallback Available? |
|---|---|---|---|---|---|---|
| [ComponentName — e.g., customer_db] | [L1] | [sync / async] | [TCP/PostgreSQL wire protocol] | [99.99% / P99 < 50ms / < 0.01% error] | [Yes — Resilience4j / No] | [Yes — cached read / No — hard fail] |
| [ComponentName — e.g., rabbitmq-prod-cluster] | [L4] | [async — publish] | [AMQP 0-9-1] | [99.9% / P99 < 20ms / < 0.1% error] | [Yes — publisher confirm timeout / No] | [Yes — local retry queue / No] |
| [ComponentName — e.g., payment-svc] | [L5] | [sync — REST] | [HTTPS / JSON] | [99.5% / P99 < 300ms / < 0.5% error] | [Yes — Hystrix / No] | [No — payment cannot proceed without] |
| [add rows for every dependency] | | | | | | |

**Dependency count:** [N] upstream dependencies

---

## Section 3: Downstream Consumers (what calls this service)

List every service or client that calls this service. Include batch jobs, event consumers, and internal tooling. Do not omit anything.

| Consumer Name | Layer | Consumption Type | Expected Call Rate | Consumer SLO | Does consumer have fallback if this service fails? |
|---|---|---|---|---|---|
| [ComponentName — e.g., kong-gateway-prod] | [L6] | [sync — routes external traffic here] | [~5,000 req/min peak] | [99.99% / P99 < 50ms / < 0.01% error] | [No — gateway cannot serve this route without upstream] |
| [ComponentName — e.g., reporting-batch-job] | [L5] | [async — nightly batch read] | [1x/night, 10K records] | [95% success rate / 4hr SLA] | [Yes — retries for up to 3 hours before alerting] |
| [ComponentName — e.g., mobile-bff-svc] | [L6] | [sync — REST fan-out] | [~2,000 req/min peak] | [99.5% / P99 < 400ms / < 0.5% error] | [Partial — returns cached profile, no live data] |
| [add rows for every consumer] | | | | | |

**Consumer count:** [N] downstream consumers

---

## Section 4: SPOF Risk Assessment

Answer each question directly. These answers are read by the VP during architecture reviews and incident bridges. Be explicit — "unknown" is a valid answer but must be followed up within 48 hours.

**4a. Is there any upstream dependency with no HA configuration and no consumer-side fallback?**
Answer: [Yes / No]
If Yes: [Name the dependency, describe the failure mode, and state the mitigation gap]

**4b. Is there any downstream consumer that would completely fail (not degrade — fail) if this service is unavailable?**
Answer: [Yes / No]
If Yes: [Name the consumer and describe the failure mode — e.g., "kong-gateway cannot route /orders — 100% of order placement requests return 502"]

**4c. What is the blast radius if this service fails completely?**
- Direct consumers impacted: [N services]
- Estimated user-facing impact: [e.g., "100% of order placement, 30% of profile reads — affects approximately 50K active sessions at peak"]
- Estimated revenue impact: [e.g., "$X per minute at peak" or "unknown — add to next PIR action items"]
- Secondary blast (consumers of your consumers): [e.g., "mobile app and web portal lose order functionality"]

**4d. Is this service on the critical path for any SEV1-eligible user journey?**
Answer: [Yes / No]
If Yes: [Name the user journey — e.g., "order placement, checkout completion, payment processing"]

**4e. What is the Recovery Time Objective (RTO) if this service fails and auto-recovery does not trigger?**
[State in minutes — include pod restart time, dependency reconnect time, and warm-up time if applicable]

**4f. What data is at risk if this service fails mid-transaction? (RPO)**
[Describe any in-flight data loss risk — e.g., "orders submitted in the 500ms window between DB write and RabbitMQ publish may be lost — no compensating transaction exists"]

---

## Section 5: Dependency Map Diagram (ASCII)

Draw this service in the center. Upstream dependencies on the left. Downstream consumers on the right. Include layer labels. Draw arrows in the direction of the call (caller → callee).

```
Template format:

[UpstreamDep1:L1] ──────────────────────────────────────────┐
                                                             │
[UpstreamDep2:L4] ──────────────→ [THIS-SERVICE:LN] ───────→ [Consumer1:L6] → External Users
                                          │
[UpstreamDep3:L5] ──────────────────────┘         ───────→ [Consumer2:L5]
                                                   │
                                                   └───────→ [Consumer3:L5]
```

Example (OrderService at L5):

```
[customer_db:L1] ─→ [customer-data-svc:L3] ─┐
                                              │
[rabbitmq-prod:L4] ──────────────────────────→ [order-svc:L5] ──→ [kong-gateway:L6] → External Consumers
                                              │
[payment-svc:L5] ────────────────────────────┘       └──async──→ [notification-svc:L5]
```

**Your diagram for [SERVICE NAME]:**

```
[paste your ASCII diagram here]
```

---

## Section 6: Dependency Map Maintenance

| Field | Value |
|---|---|
| Review frequency | [Quarterly minimum — or after any architectural change] |
| Last reviewed | [YYYY-MM-DD] |
| Reviewed by | [name + role] |
| Next scheduled review | [YYYY-MM-DD] |
| Open gaps | [list any dependency or consumer that is listed as "unknown" or unverified] |

**Maintenance rule:** Any dependency or consumer discovered during an incident that is not on this map must be added before the PIR is closed. The PIR owner is responsible for updating this document.
